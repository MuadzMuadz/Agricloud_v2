<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>AgriCloud API</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: #0f172a;
            color: #e2e8f0;
            display: flex;
            min-height: 100vh;
            margin: 0;
            align-items: center;
            justify-content: center;
        }
        .card {
            text-align: center;
            padding: 2rem;
        }
        h1 { margin: 0 0 .5rem; font-size: 1.75rem; }
        p { margin: .25rem 0; color: #94a3b8; }
        code {
            background: #1e293b;
            padding: .15rem .4rem;
            border-radius: .25rem;
            color: #38bdf8;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>🌱 AgriCloud API</h1>
        <p>Backend REST API berjalan normal.</p>
        <p>Health check: <code>GET /up</code></p>
        <p>Base API: <code>/api</code></p>
    </div>
</body>
</html>
